import { SiderProps } from 'antd'
import styled from 'styled-components'
import Sidebar from './Sidebar'

export const StyledSidebar: React.FC<SiderProps> = styled(Sidebar)`
  background-color: white;
`
